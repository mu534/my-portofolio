{
    "name": "my-portfolio",
    "version": "1.0.0",
    "description": "A portfolio project",
    "main": "index.js",
    "scripts": {
      "build": "echo Building project... && npm install && npm run build:assets",
      "start": "serve ./dist",
      "build:assets": "parcel build index.html --out-dir dist"
    },
    "dependencies": {},
    "devDependencies": {
      "parcel-bundler": "^1.12.4"
    }
  }
  